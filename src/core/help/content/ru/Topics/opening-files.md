@source-sha 8bfbc3d43887707b6a34bc4585de3f4ba8ccd95c7c446a2506a2717dbf579214
# Открытие файлов: A и B

> На рабочем месте два слота. Один файл — обычный редактор, два — ещё и сравнение. Править можно в обеих панелях.

На рабочем месте два файловых слота, **Файл A** и **Файл B**. От того, в какой слот попал файл, зависит поведение:

- **Открыт один файл** — однофайловый режим. Рабочее место работает как hex-редактор этого файла: правка, поиск и панели прошивок работают как обычно.
- **Открыты два файла** — режим сравнения. Дампы стоят рядом (или один над другим, см. **Вид ▸ Расположить панели рядом**), и каждый различающийся байт закрашен.

Файл B необязателен. Второй файл нужен только самому сравнению.

## Чем открыть дамп

- **Кнопкой открытия** на пустом экране или пунктом **Файл ▸ Открыть…** в меню тулбара.
- **Перетаскиванием.** Тащите файл на рабочее место — полосы показывают, куда он ляжет: в эту панель или рядом с ней. Два файла сразу заполнят оба слота.
- **Файл ▸ Сравнить с…** открывает второй дамп в свободный слот — это сравнение в один шаг.
- **Файл ▸ Новый файл** создаёт пустой безымянный файл, куда можно вставить байты.

## О чём браузер спрашивает и о чём спросит снова

Программа ничего никуда не загружает: открытый файл читается внутри этой вкладки, а байты остаются на вашей машине.

Взамен браузер даёт *дескриптор* выбранного файла, и живёт он ровно столько, сколько живёт страница. Отсюда:

- **У перезагруженной страницы файлов нет.** Закрытая вкладка, перезагрузка, возвращение к ней завтра — всё начинается с пустого экрана, и дампы придётся открыть заново.
- **Разрешение спрашивают один раз на файл за визит.** В браузере на Chromium запись в открытый файл может спросить разрешение второй раз — это вопрос браузера, а не программы.

! Держите дампы в папке, которую сможете найти. Программа не откроет вчерашний файл сама: веб-странице никогда не сообщают, где файл лежит.

## Одна работа — одна вкладка браузера

Внутри программы вкладок нет, второго окна тоже: **одно рабочее место — одна вкладка браузера**. Так и держат несколько плат на одном экране: откройте программу в другой вкладке, и у неё будут свои два слота, свои закладки и своя отмена.

В заголовке каждой панели — имя файла и признак несохранённых изменений; размер — в строке состояния под ним. ✕ в заголовке закрывает эту панель и оставляет вторую открытой.

## Если файл уже открыт

Открыть файл, который уже лежит в другом слоте, можно — сравнивать файл с самим собой, правя одну из копий, вполне законно. Открытие в тот же слот, где он и так лежит, не делает ничего.

! Замена панели с несохранёнными правками спросит заранее. Отмены у выброшенной панели нет.

См. также: [[topic:saving|Сохранение]], [[topic:join-duplicate|Объединение и дублирование]], [[topic:large-files|Большие дампы]].
